<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>G Test</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <%- include('navbar') %>
    <div class="container">
        <h1>G Test</h1>
        <% if (!user) { %>
            <form action="/get-user" method="POST">
                <label for="licenseNumber">License Number:</label>
                <input type="text" id="licenseNumber" name="licenseNumber" maxlength="8" pattern="[A-Za-z0-9]{8}" required>
                <button type="submit">Submit</button>
            </form>
        <% } else { %>
            <h2>User Information</h2>
            <p><strong>First Name:</strong> <%= user.firstName %></p>
            <p><strong>Last Name:</strong> <%= user.lastName %></p>
            <p><strong>Age:</strong> <%= user.age %></p>
            <p><strong>License Number:</strong> <%= user.licenseNumber %></p>
            <h3>Car Details</h3>
            <form action="/update-car" method="POST">
                <input type="hidden" name="licenseNumber" value="<%= user.licenseNumber %>">
                <label for="make">Make:</label>
                <input type="text" id="make" name="make" value="<%= user.carDetails.make %>" required>
                
                <label for="model">Model:</label>
                <input type="text" id="model" name="model" value="<%= user.carDetails.model %>" required>
                
                <label for="year">Year:</label>
                <input type="number" id="year" name="year" value="<%= user.carDetails.year %>" required>
                
                <label for="plateNumber">Plate Number:</label>
                <input type="text" id="plateNumber" name="plateNumber" value="<%= user.carDetails.plateNumber %>" required>
                
                <button type="submit">Update</button>
            </form>
        <% } %>
        <% if (error) { %>
            <h2><%= error %></h2>
            <a href="/g2">Go to G2 Page</a>
        <% } %>
    </div>
</body>
</html>
